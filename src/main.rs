fn main() {
    println!("Hello World");
}

/*
 * find a way to access all files in a directory.
 * read data from all the files.
 * store the term frequency
 * store the document frequency
 * somehow apply TF-IDF (term frequency - inverse document frequency)
 */
